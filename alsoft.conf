# OpenAL Soft on ALSA-only firmwares (ArkOS/ROCKNIX without PulseAudio).
# The mmap path stalls with "mmap commit error: Broken pipe" in long sessions
# on RK3326-class handhelds; the same fix the approved Bully port ships.
[general]
periods = 4
period_size = 1024

[alsa]
mmap = false
